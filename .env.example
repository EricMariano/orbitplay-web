# Base URL the API client prefixes onto every request.
# In dev this stays relative so Vite proxies /api -> http://localhost:3000
# (same-origin, so the httpOnly session cookie is sent). See vite.config.ts.
VITE_API_URL=/api
